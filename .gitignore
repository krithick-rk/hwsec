node_modules/
hwsec-output/
hwsec-cli-test-output/
dummy_rtl/
docx_content.txt
read_docx.mjs
test_env.py
bad_config.json
test_unrelated/

# Local secrets — never commit real secrets or credentials
.env
.env.local
config.json

# One-off test scripts and scratch files
scratch_*.mjs
scratch_*.json
test_phase6.js
test_run.bat
test.c
verify.sc
run_joern.sh
test_empty_dir/
testfiles_operational_gate/

# Temporary and extraction artifacts
temp.zip
temp_docx/
new_docx_content.txt

# Runtime output and databases
hwsec-live-audit-run/
workspace/
*.db
*.log

# JVM crash logs (generated during development/testing with Java tools)
hs_err_pid*.log
replay_pid*.log

# Machine-local architecture/status snapshots (developer-only)
architecture_status.json

# Experimental telemetry artifacts (contain partial API key hints)
tests/experimental/provider_telemetry.json
tests/experimental/experiment_results.json
tests/experimental/experiment_freeze.json

# Python cache
tests/experimental/__pycache__/
**/__pycache__/
*.pyc

# Vulnerability benchmark datasets and evidence runs (large local archives)
quality-benchmark/c/
quality-benchmark/cpp/
quality-benchmark/cross-project/
quality-benchmark/evidence/
quality-benchmark/go/
quality-benchmark/hardware/
quality-benchmark/java/
quality-benchmark/manifests/
quality-benchmark/python/

# Experiment run artifacts — CodeQL binary databases, compiled blobs, binary fixtures
# Source-level reports and configs are tracked; binary DB internals are not
experiments/**/tools/codeql/*/db-*/
experiments/**/tools/codeql/*/db-*/**
tests/fixtures/target_bin
tests/fixtures/target_sim
tests/fixtures/multilang_project/target_sim

# Binary/compiled outputs that are regenerated on demand
*.o
*.so
*.elf
*.bin
