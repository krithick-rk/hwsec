# Fixed Test Cases Index

- `command_injection_fixed.py`: Input validated via `ipaddress.ip_address` and `shell=False`.
- `buffer_overflow_fixed.c`: Bounded string copy via `strncpy`.
- `PathTraversalFixed.java`: Path normalization and root container check `startsWith`.
- `secret_leak_fixed.v`: Redacted debug bus signal `32'h00000000`.
